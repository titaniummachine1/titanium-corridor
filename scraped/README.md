# Scraped source (readable)

Raw minified bundle: `index-xQ3tC4A2.js` (583 KB, single line).

These files are **deobfuscated extracts** — same logic, readable names and formatting:

| File                       | Original class   | What it is                          |
| -------------------------- | ---------------- | ----------------------------------- |
| `game_logic_extract.js`    | `p9`             | Board rules, moves, walls, notation |
| `engine_config_extract.js` | `f7`, `_7`, `k7` | Engine URLs, enums, position format |
| `engine_client_extract.js` | `mT`             | WebSocket client + Redux wiring     |

The AI engine itself is **not here** — it runs at `wss://quoridor-ai.com/*`.
